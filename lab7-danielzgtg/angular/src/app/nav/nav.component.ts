import { Component } from '@angular/core';

@Component({
  selector: 'app-nav',
  templateUrl: './nav.component.html',
  styleUrls: ['./nav.component.scss']
})
export class NavComponent {
  // Bootstrap doesn't work well. It doesn't match the show screenshot in the assignment so it was removed.
}
